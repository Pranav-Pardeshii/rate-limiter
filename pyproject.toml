[project]
name = "rate-limiter"
version = "0.1.0"
description = "Token Bucket rate limiter"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.0",
    "uvicorn>=0.50.2",
]
